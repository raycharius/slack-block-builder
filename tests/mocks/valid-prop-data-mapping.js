const types = require('./data-mocks');

module.exports = {
  blocks: types.arrayBlocks,
  actionId: types.string,
  blockId: types.string,
  title: types.string,
  text: types.string,
  confirm: types.confirmationDialog,
  confirmDialog: types.string,
  maxSelectedItems: types.int,
  deny: types.string,
  primary: types.primary,
  danger: types.danger,
  label: types.string,
  options: types.arrayOptions,
  value: types.string,
  description: types.string,
  url: types.string,
  elements: types.arrayElements,
  externalId: types.string,
  imageUrl: types.string,
  altText: types.string,
  element: types.element,
  hint: types.string,
  optional: types.bool,
  fields: types.arrayStrings,
  accessory: types.element,
  initialChannels: types.arrayStrings,
  initialChannel: types.string,
  responseUrlEnabled: types.bool,
  initialOptions: types.arrayOptions,
  initialConversations: types.arrayStrings,
  defaultToCurrentConversation: types.bool,
  filter: types.arrayStrings,
  excludeExternalSharedChannels: types.bool,
  excludeBotUsers: types.bool,
  initialConversation: types.string,
  initialDate: types.date,
  minQueryLength: types.int,
  initialOption: types.option,
  optionGroups: types.optionGroups,
  placeholder: types.string,
  initialValue: types.string,
  multiline: types.bool,
  minLength: types.int,
  maxLength: types.int,
  initialUsers: types.arrayStrings,
  initialUser: types.string,
  channel: types.string,
  close: types.string,
  submit: types.string,
  clearOnClose: types.bool,
  notifyOnClose: types.bool,
  privateMetaData: types.string,
  callbackId: types.string,
  asUser: types.bool,
  threadsTs: types.string,
  ts: types.string,
  replaceOriginal: types.bool,
  deleteOriginal: types.bool,
  responseType: types.ephemeral,
  postAt: types.timestamp,
  ephemeral: types.ephemeral,
  inChannel: types.inChannel,
  color: types.string,
  fallback: types.string,
  attachments: types.arrayAttachments,
  dispatchAction: types.bool,
  onEnterPressed: types.onEnterPressed,
  onCharacterEntered: types.onCharacterEntered,
  initialTime: types.time,
  ignoreMarkdown: types.falseBool,
  submitDisabled: types.bool,
};
