import { URLInputBuilder } from '../../../src/elements/url-input';

export const params = {
  placeholder: 'placeholder',
  actionId: 'actionId',
  initialValue: 'https://savelife.in.ua/en/donate/',
};

export const mock = new URLInputBuilder(params);
