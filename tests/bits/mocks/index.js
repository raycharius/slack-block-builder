const ConfirmationDialog = require('./confirmation-dialog.mock');
const Option = require('./option.mock');
const OptionGroup = require('./option-group.mock');
const Attachment = require('./attachment.mock');

module.exports = {
  ConfirmationDialog,
  Option,
  OptionGroup,
  Attachment,
}